<li>
  <%= gravatar_for user, :size => 100 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
  | <%= link_to "delete", user, :method => :delete, :confirm => "Would you like to delete this user?",
                                :title => "Delete #{user.name}" %>
  <% end %>
</li>